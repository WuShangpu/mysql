第8章 用通配符进行过滤
1.通配符用于模糊搜索
2.搜索模式是指由字面值、通配符或两者组合构成的搜索条件
3.LIKE在作谓词时不是操作符
4.%表示任意数量的字符串
5.如果一个字符串后面有一个或者多个空格，需要在搜索模式后面加上一个%。
  更好的方式是使用函数去掉首位空格（第11章将会介绍）。
6.%无法匹配NULL
7.下划线_只匹配单个字符
8.通配符搜索比其他搜索花费的时间更长。
9.把通配符置于搜索模式的开始处，搜索起来是最慢的。
10.