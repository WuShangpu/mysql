第10章 创建计算字段
计算字段的作用就是在数据库这端将检索出来的数据按照要求转换、计算、格式化。
1.拼接字段。多数DBMS使用+或者||来拼接字段，但是Mysql使用函数concat（）
  来完成这项工作。
2.删除数据右边多余的空格可使用函数RTrim（）。还有LTrim（）和Trim（）
3.利用AS取别名，放在SELECT语句后面
4.AS可以重新命名表列名
5.别名有时也称为导出列
6.SELECT Now（）用来检测当前的时间
7.SELECT 可以省略FROM子句以便简单地访问和处理表达式


